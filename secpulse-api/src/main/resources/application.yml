spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/secpulse
    username: secpulse
    password: secpulse
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    location: classpath:db/migration